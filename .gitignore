# API keys — never commit these
.env
.env.local
.env.*.local

node_modules/
.DS_Store
Thumbs.db
*.log

data/

# Built for distribution, rebuilt on demand — not source
sot-extension.zip

# Built extension packages — rebuilt with npm run build:ext
dist/
